non-fluents inventory_2nf {
	domain = inventory;

	objects{
		item: {i1, i2};
	};
	
	non-fluents {
		C(i2) = 0.5;
		P(i2) = 1.5;
		MEAN(i2) = 5.0;
		DISP(i2) = 2.0;
	};
}

instance inventory_1 {
	domain = inventory;
	non-fluents = inventory_2nf;

	init-state {
		stock(i1) = 0;
		stock(i2) = 0;
	};

	max-nondef-actions = pos-inf;
	horizon = 8;
	discount = 1.0;
}
