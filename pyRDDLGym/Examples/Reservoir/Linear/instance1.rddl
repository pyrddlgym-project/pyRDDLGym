non-fluents Reservoir_2nf {
	domain = reservoir_control_linear;

	objects{
		id: {t1, t2};
	};

	non-fluents {
		RAIN_MEAN(t2) = 10.0;
		RAIN_VAR(t2) = 10.0;
		LOW_BOUND(t2) = 30.0;
		HIGH_BOUND(t2) = 180.0;
		MAXCAP(t2) = 200.0;
		DOWNSTREAM(t1, t2);
	};
}

instance Reservoir_linear_1 {
	domain = reservoir_control_linear;
	non-fluents = Reservoir_2nf;

	init-state{
		rlevel(t1) = 75.0;
		rlevel(t2) = 150.0;
	};

	max-nondef-actions = pos-inf;
	horizon = 10;
	discount = 1.0;
}
